<% layout("/layouts/boilerplate") %>
<div class="row mt-5">
    <div class="col-8 offset-3">

    <h3> Listing Details</h3>
    </div>
    <div class="card col-6 offset-3">
        <img src="<%= listing.image %>" class="card-img-top show-img" alt="Listing Image">
  <div class="card-body">
    <p class="card-text">
       <b> <%= listing.title %> </b> <br>
        <%= listing.description %><br>
&#8377 <%= listing.price.toLocaleString("en-IN") %><br>  <!-- 8377 for rupee symbol or tolocaleString pass valuse in string as idian curreency-->
        <%= listing.location %><br>
        <%= listing.country %>
</p>
  </div>
    </div>
        </form>
</div>
<div class="btns">
    <a href="/listings/<%= listing._id %>/edit" class="btn btn-dark col-1 offset-3 add-btn"> Edit</a>

    <form method="post" action="/listings/<%= listing._id %>?_method=Delete">
        <button class="btn btn-dark offset-4 ">Delete</button>
</div>
   
